params {
    container = "quay.io/nextflow/bash:latest"
}

weblog {
    enabled = true
    //url = "echoserver:3000"               // Missing prefix breaks
    url = "http://echoserver:3000"
    basicToken = "fakeuser:fakepassword"    // Dont B64-encode
}

plugins {
    // https://github.com/nextflow-io/nf-weblog
    id 'nf-weblog'
}