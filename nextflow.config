process.container = 'quay.io/nextflow/bash'

profiles {

    profile A {
        params.foo = "foo"
        includeConfig 'conf/profileAA.conf'
    }

    profile B {
        params.bar = "bar"
        includeConfig 'conf/profileBB.conf
    }
}